import { FC } from "react";

interface AdsSpaceProps {}

const AdsSpace: FC<AdsSpaceProps> = ({}) => {
  return (
    <div className="flex justify-center bg-[#FEEFEA] py-10 border border-dashed border-black">
      مساحه اعلانية
    </div>
  );
};

export default AdsSpace;
